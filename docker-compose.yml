services:
  chroma:
    image: chromadb/chroma:latest
    container_name: cloudpilot-chroma
    ports:
      - "8000:8000"
    volumes:
      - chroma-data:/data
    restart: unless-stopped

volumes:
  chroma-data:
